const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  React.createElement(
    "h1",
    { style: { color: "green", fontFamily: "monospace" } },
    "✅ REACT IS RENDERING"
  )
);
``
